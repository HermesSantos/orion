isPalindrome (integer n) {
    [integer] nArr = n.toArray()
    [integer] new = []
    for index, value :: nArr {
        new.push(nArr[nArr.length() - 1 - index])
    }
    io::write(new)
}

isPalindrome(12)